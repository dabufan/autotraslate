{
  "name": "autotranslate-extension-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Auto-translate any foreign webpage to the user's preferred language — browser extension MVP (MV3).",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prepare": "echo \"Run: npm install && npm run build\"",
    "build:zip": "node tools/publish/make-zip.mjs",
    "publish:chrome": "node tools/publish/chrome/publish.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1",
    "archiver": "^7.0.1",
    "@types/chrome": "^0.0.262"
  }
}
